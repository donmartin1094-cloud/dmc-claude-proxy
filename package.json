{
  "name": "dmc-claude-proxy",
  "version": "1.0.0",
  "description": "Claude API proxy for DMC app — no timeout limits",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
